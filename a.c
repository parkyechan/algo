#include<string.h>
#include<stdlib.h>
main(){
  int a, b;
  char **arr = (char *)malloc(sizeof(char) * 500000);

  scanf("%d %d", &a, &b);
  for(int i=0; i<a+b; i++){
    scanf("%s", arr[i])
  }
  for(int i=0; i<a; i++){
    
  }
}

#include<string.h>
main(){
  char arr[1001] = {};
  scanf("%s", arr);
  int sum1 = 0, sum2 = 0;
  int res;
  for(int i=0; i<strlen(arr); i++){
    arr[i] -= 48;
  }
  for(int i=0; i<strlen(arr); i++){
    for(int j=i; j<(strlen(arr) - i)/2; j++){
      for(int k=j; k<(j+1) * 2; k++){

      }
      for(int k=(j+1)*2; k<)
    }
  }
}
